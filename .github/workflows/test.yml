name: OS test
on: [push]
jobs:
    OSTest:
        strategy:
            matrix:
                os: [ubuntu-latest, windows-latest, macos-latest]
        runs-on: ${{ matrix.os }}
        steps:
            - name: say hello
              run: echo "Hello from GitHub Actions"
